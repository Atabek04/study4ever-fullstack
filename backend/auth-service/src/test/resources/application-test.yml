spring:
  datasource:
    url: jdbc:tc:postgresql:14:///testdb
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false

app:
  jwt:
    secret: testsecrettestsecrettestsecrettestsecrettestsecrettestsecret
    access-token-expiration-ms: 300000
    refresh-token-expiration-ms: 600000
    issuer: test-auth-service

eureka:
  client:
    enabled: false